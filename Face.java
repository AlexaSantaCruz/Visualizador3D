class Face {
    int[] vertexIndices;
    int[] textureIndices;

    Face(int[] vertexIndices, int[] textureIndices) {
        this.vertexIndices = vertexIndices;
        this.textureIndices = textureIndices;
    }
}